import {Category} from "./category";

export class PageCategory{
      content?: Category[];
      pageNumber?: number;
      pageSize?: number;
      totalElements?: number;
      totalPages?: number;
}
